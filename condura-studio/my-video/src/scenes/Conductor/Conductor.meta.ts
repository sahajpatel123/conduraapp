// src/scenes/Conductor/Conductor.meta.ts

import type { SceneSpec } from "../../config/types";

export const conductorMeta: SceneSpec = {
  id: "conductor",
  title: "The Conductor",
  durationInFrames: 300,
};
